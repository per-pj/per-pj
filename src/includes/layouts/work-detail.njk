---
layout: layouts/base.njk
---

<section class="work-detail-hero-container">
  <div class="work-detail-hero-content">
    <div class="work-detail-hero-tags">
      {% for tag in tags %}{% if tag != "work" %}
        <span class="tag-large">{{ tag }}</span>
      {% endif %}{% endfor %}
    </div>
    <h1 class="work-detail-hero-title">{{ title }}</h1>
    <p class="work-detail-hero-desc">{{ description }}</p>
    <img src="{{ thumbnail | url }}" alt="{{ title }}" class="work-detail-hero-img" />
  </div>
</section>

<section class="work-detail-content-container">
  <div class="work-detail-content">
    {{ content | safe }}
  </div>
</section>


